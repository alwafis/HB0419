
import {
  StyleSheet
} from "react-native";
import {config} from '../../config/global';


const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 10
  }
});

export default styles;